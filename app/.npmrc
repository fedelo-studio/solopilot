# pnpm/npm config for SoloPilot.
# Required because React 19 + Next 15 + a few peer deps still bundle older peer
# ranges that fail under pnpm's strict checks by default.

auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=true
